import type { KnipConfig } from 'knip';

const config: KnipConfig = {
  workspaces: {
    '.': {
      ignoreDependencies: ['@biomejs/biome', '@changesets/cli', 'knip', 'jscpd'],
    },
    'apps/webapp': {
      entry: ['scripts/*.ts'],
      ignore: [
        'src/payload-types.ts', // auto-generated by `payload generate:types`
      ],
      ignoreDependencies: [
        'dotenv', // loaded via next.config / payload.config at runtime
      ],
    },
    'packages/sdk': {
      entry: ['src/index.ts', 'scripts/*.ts'],
      project: ['src/**/*.ts'],
    },
  },
};

export default config;
